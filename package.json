{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "Portfolio Page",
  "scripts": {
    "start": "rm -rf dev-serve && parcel src/html/index.html --dist-dir dev-serve",
    "build": "rm -rf build && parcel build src/html/index.html --dist-dir build --public-url ./",
    "publish": "gh-pages -d build"
  },
  "author": "bd",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.7.0",
    "gh-pages": "^4.0.0",
    "parcel": "^2.7.0"
  },
  "dependencies": {
  }
}
